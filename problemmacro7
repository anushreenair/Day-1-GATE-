#include<stdio.h>

#define MUL(a,b) ((a) * (b))

int main () {
    int x = 2; 
    int y = 3; 
    int z = MUL(x +1, y+2);
    printf("%d\n", z);
    return 0;
    
}

/*
((x + 1) * (y + 2)) → ((2 + 1) * (3 + 2)) = 3 * 5 = 15
*/